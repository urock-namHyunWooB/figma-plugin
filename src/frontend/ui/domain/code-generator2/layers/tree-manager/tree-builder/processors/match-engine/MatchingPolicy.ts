/**
 * Task A1 임시 스텁 — Task A2에서 정식 타입으로 교체 예정.
 *
 * MatchSignal.ts의 `import type { MatchingPolicy }`가 모듈 해석 실패로
 * 컴파일 에러를 내지 않도록 하기 위한 placeholder.
 *
 * @deprecated Task A2에서 실제 MatchingPolicy 인터페이스로 대체된다.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type MatchingPolicy = any;
